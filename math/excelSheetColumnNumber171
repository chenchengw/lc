  public int titleToNumber(String s) {
        int i = 0, result = 0;
        while (i < s.length()) {
            result *= 26;
            result += s.charAt(i) - 'A' + 1;
            i++;
        }
        return result;
    }
